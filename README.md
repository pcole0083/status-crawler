# status-crawler
Crawls URLs and writes successes and failures to text files

Uses my own fork of js-crawler to write more than just url addresses to success and failure files.
Added logging of image and link urls.
